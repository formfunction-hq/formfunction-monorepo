CREATE TABLE "public"."ArtistSubmission" ("id" uuid NOT NULL DEFAULT gen_random_uuid(), "userId" text NOT NULL, "websiteUrl" text NOT NULL, "twitterName" text NOT NULL, "instagramName" text, "discordHandle" text NOT NULL, "artistStatement" text NOT NULL, "isCopyrightVerified" boolean NOT NULL, PRIMARY KEY ("id") , FOREIGN KEY ("userId") REFERENCES "public"."User"("id") ON UPDATE restrict ON DELETE restrict, UNIQUE ("id"), UNIQUE ("userId"));COMMENT ON TABLE "public"."ArtistSubmission" IS E'Each row contains an artist\'s application submission';
CREATE EXTENSION IF NOT EXISTS pgcrypto;
