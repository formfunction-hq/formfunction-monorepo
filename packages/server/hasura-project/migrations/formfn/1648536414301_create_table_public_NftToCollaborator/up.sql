CREATE TABLE "public"."NftToCollaborator" ("id" uuid NOT NULL DEFAULT gen_random_uuid(), "collaboratorId" text NOT NULL, "nftId" text NOT NULL, "share" integer NOT NULL, "verified" boolean NOT NULL, "ignored" boolean NOT NULL, PRIMARY KEY ("id") , FOREIGN KEY ("collaboratorId") REFERENCES "public"."User"("id") ON UPDATE cascade ON DELETE cascade, FOREIGN KEY ("nftId") REFERENCES "public"."Nft"("id") ON UPDATE cascade ON DELETE cascade, UNIQUE ("id"), UNIQUE ("collaboratorId", "nftId"));COMMENT ON TABLE "public"."NftToCollaborator" IS E'An NFT may have multiple "creators," with whom primary and secondary sales are split. We call these people "collaborators".';
CREATE EXTENSION IF NOT EXISTS pgcrypto;
