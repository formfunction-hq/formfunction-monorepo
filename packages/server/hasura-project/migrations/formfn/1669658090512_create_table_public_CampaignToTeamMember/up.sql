CREATE TABLE "public"."CampaignToTeamMember" ("id" uuid NOT NULL DEFAULT gen_random_uuid(), "campaignId" uuid NOT NULL, "memberId" text NOT NULL, "timeCreated" timestamptz NOT NULL DEFAULT now(), "requestId" uuid NOT NULL, "role" text NOT NULL, PRIMARY KEY ("id") , FOREIGN KEY ("campaignId") REFERENCES "public"."Campaign"("id") ON UPDATE cascade ON DELETE cascade, FOREIGN KEY ("requestId") REFERENCES "public"."Request"("id") ON UPDATE cascade ON DELETE cascade, FOREIGN KEY ("memberId") REFERENCES "public"."User"("id") ON UPDATE cascade ON DELETE cascade, UNIQUE ("id"), UNIQUE ("requestId"));
CREATE EXTENSION IF NOT EXISTS pgcrypto;
